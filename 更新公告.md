# 更新公告

這裡集中放聽雪居每個版本的更新內容與 APK 資訊。README 只保留下載與專案介紹，詳細版本紀錄放在這一頁。

## v1.7

這版加入通用 OpenAI-compatible 思考內容顯示，並簡化 API Key 輸入欄位，方便確認自己是否已填入金鑰。

- 支援所有 OpenAI-compatible 串流回傳中的 `delta.reasoning_content`。
- 放寬思考內容解析白名單，額外支援 `reasoning`、`thinking`、`thought` 與 `provider_specific_fields` 內的常見思考欄位。
- 思考內容會保存到 `generation_contexts.reasoningContent`，不寫入 `MessageEntity.content`。
- 思考內容不會進入下一輪上下文、摘要或 World Info 關鍵詞掃描。
- AI 訊息正文上方新增「思考內容」折疊區，預設收合。
- 思考區可單獨複製內容；一般複製、編輯、重發仍只處理正文。
- 編輯 AI 正文後會清空該訊息舊的思考內容，避免內容不對應。
- Room 資料庫升到 v6，新增 `reasoningContent` 欄位。
- API Key 輸入欄位改為直接顯示文字，不再使用密碼遮罩。
- Agnes 既有 `chat_template_kwargs.enable_thinking = true` 保留。
- Android 版本資訊更新為 `versionName = 1.7`、`versionCode = 8`。

驗證：

- `testDebugUnitTest --rerun-tasks` 通過。
- `assembleDebug` 通過。
- Android 模擬器安裝成功，`MainActivity` 正常在前景。
- `logcat` 未見 `FATAL EXCEPTION`。

APK：

- `apk/TingXueJu-v1.7-debug.apk`
- `apk/TingXueJu-debug.apk`

SHA-256：

```text
22CF6B26DE2206DE930D67B65A9311AC92D5B37695A9A31E2298FC8575E2F35D
```

## v1.6

本次完成資料層、UI、ViewModel、導航與 Domain/use case 架構整理，同時修正 World Info 觸發問題，並加入 Agnes 相關預設設定。

- 新增 Repository 層：`ConversationRepository`、`ProfileRepository`、`WorldInfoRepository`。
- `MainViewModel` 不再直接操作 DAO，資料存取統一走 Repository。
- 拆分原本過大的 `AIChatApp.kt`，將 UI 依功能整理到多個 `ui/` 檔案。
- 拆分原本過大的 ViewModel：`ChatViewModel`、`ProfilesViewModel`、`WorldSetsViewModel`、`NewChatViewModel`、`SettingsViewModel`。
- 新增 `AppContainer` 與 `AppViewModelFactory`，移除 `AndroidViewModel` 依賴。
- 導航改由 App 層集中處理，移除舊 `MainViewModel` 作為導航中繼的做法。
- 新增 Domain/use case 層，處理聊天串流、對話摘要、AI 整理角色卡、AI 整理世界設定、匯入世界設定儲存與模型列表載入。
- 修正 World Info 命中問題：世界設定關鍵詞現在會掃描完整對話 history，不再因摘要或 context 裁切導致舊訊息關鍵詞失效。
- World Info 紅燈測試已轉綠。
- `Settings` 改用 `SettingsUiState` 整合設定、模型列表、載入狀態與錯誤狀態。
- 匯入文件整理流程抽出共用邏輯，角色卡與世界設定共用 chunk + AI 整理流程。
- 對話摘要流程抽離 ViewModel，降低聊天邏輯耦合。
- 新增 Agnes 預設 API 端點與模型設定，預設支援 `agnes-2.0-flash`。
- Agnes 預設送出 `chat_template_kwargs.enable_thinking = true`，開啟思考模式相關設定。
- Android 版本資訊更新為 `versionName = 1.6`、`versionCode = 7`。

驗證：

- `assembleDebug` 通過。
- `testDebugUnitTest`：41 tests，0 failed。
- Android 模擬器實測：App 可正常安裝與啟動。
- `MainActivity` 正常在前景。
- `logcat` 無 `FATAL EXCEPTION`。
- 對話、角色、資料庫、設定、世界設定、新對話、聊天頁、對話資訊、模型選擇皆可正常開啟。

APK：

- `apk/TingXueJu-v1.6-debug.apk`
- `apk/TingXueJu-debug.apk`

SHA-256：

```text
5EE5AB950FDBE58A567333614FBE08BD445F4C1F3A00AC3BB8BB6F353153408C
```

## v1.5

這版主要整理聊天頁操作、返回邏輯與對話外觀自訂，讓長對話測試時比較順手。World Info / 世界設定仍屬測試中，建議繼續用少量對話驗證命中效果。

- 將 App 導航改成 Android 原生返回邏輯，子頁面按系統返回鍵會回到上一頁，四個主頁才退出 App。
- 聊天頁標題改成目前對話名稱，點擊標題可重新命名對話。
- 修正鍵盤呼出時上方工具列被推動的問題，讓模型與對話標題列保持在上方。
- 改善長對話 auto-follow：停在底部時穩定跟隨 AI 串流，使用者往上看舊訊息時不強制拉回底部。
- 點擊最新訊息顯示複製、編輯、重新發送時，畫面會自動把操作列帶進可視區。
- 新增聊天頁「回到底部」懸浮按鈕，離開底部時顯示，點擊後回到最新訊息並恢復串流跟隨。
- 修正回到底部與自動跟隨只跳到最新回覆開頭的問題，現在會回到聊天列表真正底部。
- 對話資訊頁新增「對話框透明度」拉桿，可依單一對話調整聊天氣泡背景透明度。
- Room 資料庫升到 v5，對話新增 `messageBubbleOpacity` 欄位。
- Android 版本資訊更新為 `versionName = 1.5`、`versionCode = 6`。

APK：

- `apk/TingXueJu-v1.5-debug.apk`
- `apk/TingXueJu-debug.apk`

SHA-256：

```text
FE72BA47246D83CCEF578684B45395CE2DA2FC83CDC39C472C7DD44FC77EF39F
```

## v1.4

這版改善聊天操作手感，補上系統返回鍵行為，並加入手動壓縮對話內容。World Info / 世界設定功能仍標記為測試中，建議先用本機對話多測幾輪再當成穩定工作流。

- 發送訊息後會自動收起鍵盤。
- Android 系統返回鍵會跟 App 內返回鍵一致；四個主頁才保留退出 App 行為。
- 底部「對話 / 角色 / 資料庫 / 設定」支援左右滑動切換，底欄固定不跟著頁面滑動。
- 訊息操作改成輕點訊息氣泡後才顯示複製、編輯與重新發送。
- World Info / 世界設定（測試中）：修正設定集儲存、條目保留、對話綁定與命中顯示。
- 世界設定集新增「一句話概括」，沒有命中特定條目時也會把世界大方向送給模型。
- 新增世界觀模板入口，並改善世界設定條目的開關文案。
- 世界設定集名稱、概括與掃描訊息數改成自動保存。
- 對話資訊頁新增手動壓縮對話，可選壓縮範圍並自訂本次保留最近訊息數。
- 手動壓縮不刪除、不隱藏原始聊天紀錄，只影響之後送給 AI 的上下文。
- Android 版本資訊更新為 `versionName = 1.4`、`versionCode = 5`。

APK：

- `apk/TingXueJu-v1.4-debug.apk`
- `apk/TingXueJu-debug.apk`

SHA-256：

```text
828BC2B36BC773A27392A95D3B063BCED4F73A0BBE6B10865E9D23C2B4317214
```

## v1.3

- 目前發現世界設定集無法生效，請不要自行導入或始建立以免消耗個人的api額度

這版加入語言切換、聊天背景圖與更緊湊的介面，並取消底部通知，讓自用測試時畫面更乾淨。

- 設定頁新增繁體中文 / 简体中文切換。
- UI 文案、錯誤提示與內建送給模型的提示文字會跟著語言切換。
- 使用者訊息、AI 回覆、角色卡、Persona 與 World Info 內容不會被自動翻譯。
- 開啟既有對話時會自動捲到最新訊息。
- 串流生成時，若使用者停在底部附近，畫面會跟隨新內容；如果正在看舊訊息，不會強制拉回底部。
- 對話資訊頁新增單一對話背景圖上傳與移除。
- 聊天背景圖只顯示在訊息區，並加固定半透明遮罩維持可讀性。
- 全 App 上方列改為 48dp 緊湊標題列，減少 20:9 手機上的上方留白。
- 取消「設定已儲存」「已重新發送」等底部短通知。
- Room 資料庫升到 v3，對話新增 `backgroundImagePath` 欄位。
- Android 版本資訊更新為 `versionName = 1.3`、`versionCode = 4`。

APK：

- `apk/TingXueJu-v1.3-debug.apk`
- `apk/TingXueJu-debug.apk`

SHA-256：

```text
8E71C3F9BD764625051A79ABFB11E1B1199367AB9C46A31E5CA5043E942CC75E
```

## v1.2

這版把 Android 底部三大金剛鍵隱藏起來，讓聊天畫面更沉浸。

- 進入 App 後自動隱藏 Android navigation bar。
- 保留狀態列，不影響時間、電量與通知區顯示。
- 從螢幕底部上滑時，可以暫時叫出系統導覽鍵。
- Android 版本資訊更新為 `versionName = 1.2`、`versionCode = 3`。

APK：

- `apk/TingXueJu-v1.2-debug.apk`
- `apk/TingXueJu-debug.apk`

SHA-256：

```text
401BF1030CAFF5812B4A4F26AACFDBBCC90BF446F2FFC07800EBD8F0EECE6F9E
```

## v1.1

這版把聊天操作做得更順手，也針對長螢幕手機調整了底部導覽。

- 底部「對話 / 角色 / 資料庫 / 設定」改成更緊湊的 52dp 自訂底欄。
- 每則訊息新增編輯功能，可修改自己輸入的訊息，也可修改 AI 輸出的訊息。
- 每則訊息新增重新發送功能，可從指定訊息位置重新生成後續內容。
- 更新 GitHub 下載 APK。

APK：

- `apk/TingXueJu-v1.1-debug.apk`
- `apk/TingXueJu-debug.apk`

SHA-256：

```text
0FEF1E4857BAE1878851BEDDDFA9DE48A0626C9671272BD6617EC06A99804EFC
```

## v1.0

第一個上傳到 GitHub 的可用版本。

- Android 12+ 原生 Kotlin + Jetpack Compose App。
- 支援 OpenRouter、Groq、Cerebras 與自訂 OpenAI 相容端點。
- 本機聊天紀錄、API 設定、模型選擇與模型搜尋。
- 角色、Persona、世界設定集與文件匯入整理功能。
- 長對話上下文過長時，可自動摘要舊內容後重試。
- 附上 debug APK，方便直接下載安裝。

APK：

- `apk/TingXueJu-v1.0-debug.apk`

SHA-256：

```text
5D376C0DEBEAA308AD293C51FBB7BDCA35C7DC93AB7E76E3F984385C085D9F99
```
